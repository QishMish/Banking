import { BaseRepository } from '@app/common';

import { AccountParamsEntity } from '../entities';

type AccountParamsRepository = BaseRepository<AccountParamsEntity>;

export { AccountParamsRepository };
