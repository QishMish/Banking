export enum LoggingProvider {
  PINO = 'PINO',
  MORGAN = 'MORGAN',
  WINSTON = 'WINSTON',
}

export enum LogLevel {
  TRACE = 'trace',
  DEBUG = 'debug',
  INFO = 'info',
  WARN = 'warn',
  ERROR = 'error',
  FATAL = 'fatal',
}
